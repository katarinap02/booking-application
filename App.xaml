<Application x:Class="BookingApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BookingApp"
             StartupUri="View/SignInForm.xaml">
    <Application.Resources>
        <!-- Natasini stilovi-->
        <SolidColorBrush x:Key="ButtonText" Color="#EEE5E9"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#EEE5E9"/>
        <SolidColorBrush x:Key="AccentColor" Color="#6665DD"/>
        <SolidColorBrush x:Key="TextColor" Color="#3423A6"/>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource ButtonText}"/>
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="FontWeight" Value="Bold"/>

        </Style>

        <Style x:Key="HeaderStyle" TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <!-- *********************** -->
    </Application.Resources>
</Application>
