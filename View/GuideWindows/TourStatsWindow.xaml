<Window x:Class="BookingApp.View.GuideWindows.TourStatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Tour Statistics" Height="400" Width="600">
    <Grid>
        <lvc:CartesianChart Series="{Binding AgeStatistics}">
            <lvc:ColumnSeries Title="Tourists by Age" Values="{Binding}" />
        </lvc:CartesianChart>
    </Grid>
</Window>