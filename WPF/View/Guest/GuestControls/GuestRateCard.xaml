<UserControl x:Class="BookingApp.View.GuestControls.GuestRateCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookingApp.View.GuestControls"
             mc:Ignorable="d" 
             d:DesignHeight="103" d:DesignWidth="990">
    <Grid>
        <Border BorderBrush="{StaticResource AccentColorN}" BorderThickness="2" CornerRadius="30"  Background="white" Width="980" Height="103">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Label Content="{Binding RulesFollowing}" Margin="65,20,852,-54" Grid.RowSpan="2"/>
                <Label Content="{Binding Cleanliness}" Margin="192,22,725,-56" Grid.RowSpan="2"/>
                <Label Content="{Binding AdditionalComment}" Margin="411,20,36,-54" Grid.RowSpan="2"/>

            </Grid>

        </Border>
    </Grid>
</UserControl>
